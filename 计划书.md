📋 项目计划书：“WinUI 工具箱 (WinUI Toolbox)”
1. 🎯 项目目标与愿景
打造一款原生体验,功能丰富、完全本地优先的 Windows 多功能工具箱。它应集合日常开发、办公与系统维护中高频使用的小工具,成为你桌面上的“瑞士军刀”。

核心原则：

原生体验：通过 WinUIonWeb 组件库,在视觉和交互上完美复刻 Windows 11 原生应用风格。

轻量高效：利用 Tauri 2 的特性,确保应用体积小（< 10MB）、启动快、内存占用低。

隐私安全：所有核心数据处理（如文件转换、加密）均在本地完成,无需上传,保障数据隐私。

离线可用：核心功能不依赖网络,随时随地可用。

2. 🛠️ 技术栈选型与架构设计
核心框架：Tauri 2.0 - 使用 Rust 构建安全、高性能的后端,并通过系统 WebView2 渲染前端。

前端框架：Vue 3 (搭配 TypeScript),与 WinUIonWeb 的 Vue 实现完美契合。

UI 组件库：WinUIonWeb - 直接使用其 Vue 组件构建 WinUI 风格界面（如 DropDownButton、ComboBox、ToggleSwitch 等）。

构建工具：Vite - 提供极快的开发启动和热更新体验。

状态与存储：

采用 Pinia 管理全局状态（主题配置、工具历史记录、用户偏好）。

使用 IndexedDB 或 Tauri 本地插件（tauri-plugin-store）持久化存储历史输入与剪贴板记录。

Rust 关键库与后端通信：

image：用于图像处理（格式转换、缩放、水印等）。

ffmpeg-next：用于音视频处理（格式转换、剪辑、提取音频等）。

pdf：用于 PDF 合并、拆分、加密等操作。

serde：用于 JSON、YAML 等数据的序列化与反序列化。

systeminfo：用于获取系统硬件信息（CPU、内存等）。

通信方案：使用 Tauri 的 Async Commands 处理耗时任务（如视频转码、PDF 合并）避免阻塞 UI 线程；利用 Channel 或 Event 机制向前端实时推送任务进度。

3. 🧩 核心功能模块规划（含新增高频工具）
参考 TroveKit、ToolKnit 和 DevBox 等项目的设计，工具箱具体划分为五大模块：

🛠️ 1. 开发者工具集 (Dev Tools)
时间戳转换：支持 Unix 时间戳与日期时间的双向转换。

编码/解码：Base64、URL、HTML 实体编码解码。

JSON 格式化：JSON 数据的格式化、验证与压缩。

哈希/加密：MD5、SHA 系列哈希生成；AES、DES 加解密。

二维码生成/解析：生成文本、Wi-Fi 等二维码；解析二维码内容。

Cron 表达式解析器：可视化解析 Cron 表达式含义，提供未来执行时间预览。

JWT 调试与解码：本地解析、验证和生成 JSON Web Token，查看 Payload 与过期时间。

UUID / 随机数生成器：支持批量生成 UUID (v1/v4/v5)、随机密码、特定长度的安全字符串。

Regex 正则速查与测试：内置常用正则表达式（邮箱、手机号、IP等），提供实时正则匹配测试沙盒。

IP / 网络工具：本地 IP 查询、Ping 测试、端口连通性检测、子网掩码计算。

📄 2. 办公与文档处理 (Office & Document)
Markdown 编辑器：集成实时预览、语法高亮、导出 HTML 等功能。

Markdown 转 PDF / Word：支持直接将 Markdown 文档导出为排版精美的 PDF 或 Word 档。

PDF 工具集：实现 PDF 合并、拆分、加密、压缩。

表格数据转换：支持 CSV 与 JSON、Excel 之间的互相转换与轻量级表格查看。

文本处理：提供字数统计、大小写转换、正则表达式测试等工具。

敏感词/脱敏工具：对身份证、手机号、银行卡等敏感信息进行一键批量脱敏掩码处理。

文本比对 (Diff)：支持文本双栏/单栏差异对比。

🎬 3. 多媒体处理 (Multimedia)
图片格式转换：支持 JPG/PNG/WebP/AVIF 等格式的批量转换。

图片压缩：在保证画质的前提下压缩图片大小。

音视频转换与剪辑：支持常见格式转换、音频提取、音视频裁剪。

图标生成：一键生成适用于不同平台的 App Icon。

GIF 动图制作：支持将多张图片或短视频转换为高质量 GIF，支持裁剪与帧率调整。

OCR 文字识别：集成本地轻量级离线 OCR 模型，实现截图文字秒提取。

屏幕截图与录屏：内置简易截图工具（支持长截图、贴图）及区域录屏功能。

音频格式转换：独立提取视频中的音频轨道（MP3、WAV、FLAC、AAC 互转）。

🖥️ 4. 系统与效率工具 (System & Productivity)
硬件监控：实时显示 CPU、内存、磁盘、网络的使用情况。

系统清理：清理系统临时文件、浏览器缓存等。

快捷键启动：支持全局热键快速唤出工具或执行特定功能。

剪贴板历史：记录和管理剪贴板内容。

局域网快传：基于本地局域网的点对点文件传输工具。

环境变量管理：可视化查看、编辑和备份 Windows 系统环境变量。

端口占用查询：一键查看指定端口占用情况，并支持一键强行结束对应进程。

白噪音/专注计时器：内置番茄钟与本地白噪音，助力高效办公。

4. 🎨 UI/UX 设计方案
整体风格：严格遵循 Windows 11 设计语言，使用圆角、亚克力（Acrylic）和云母（Mica）材质效果。

布局结构：

左侧：可折叠的侧边栏，用于工具分类导航。

右侧：主内容区，根据选中的工具动态展示不同界面。

标题栏：自定义标题栏，包含应用图标、名称以及最小化、最大化、关闭按钮。

主题：内置浅色/深色主题，并支持“跟随系统”。

5. 🗓️ 开发路线图
第一阶段：项目搭建与基础框架 (2周)

初始化 Tauri 2 + Vue 3 + TypeScript 项目。

集成 WinUIonWeb 组件库，并实现自定义标题栏。

搭建应用主布局（侧边栏 + 内容区），实现明暗主题切换。

第二阶段：核心工具开发 (4周)

实现“开发者工具集”模块（时间戳、编码解码、JSON、Cron、JWT、哈希等）。

实现“文本与文档”模块（Markdown 编辑器、文本处理、脱敏、Diff 等）。

第三阶段：高级与多媒体功能扩展 (4-6周)

实现“多媒体处理”模块（图片/音视频转换、GIF 制作、离线 OCR 等），在 Rust 端集成 image 和 ffmpeg 库。

实现“PDF 工具集”功能。

第四阶段：系统级整合与优化 (2-3周)

实现“系统与效率工具”（硬件监控、系统清理、局域网快传、端口查询等）。

添加系统托盘 (System Tray) 常驻、开机自启与全局快捷键支持。

进行全面的性能优化与测试。

第五阶段：打包与发布 (1周)

配置 Tauri 的打包选项，生成优化的 Windows 安装包（.msi 或 .exe）。

编写用户文档和 README。

6. 💡 扩展性思考
插件机制：未来可设计简单的插件系统，允许社区贡献新的小工具。

云端同步：在保证隐私的前提下，可考虑使用端到端加密同步用户设置。

AI 集成：借鉴 ToolKnit，可集成大模型 API（用户自备 Key），提供文本润色、翻译等 AI 功能。